def distinct_powers(N):
    unique_powers = set()

    for a in range(2, N + 1):
        for b in range(2, N + 1):
            unique_powers.add(a ** b)  # Using exponentiation operator directly

    return len(unique_powers)

# Read input
N = int(input())
print(distinct_powers(N))
